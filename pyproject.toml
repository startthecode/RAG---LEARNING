[project]
name = "tender-scrapper"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Ashish", email = "ashugola1122000@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "ipykernel>=7.3.0",
    "langchain>=1.0.0",
    "langchain-community>=0.4.1",
    "langchain-core>=1.0.0",
    "pymupdf>=1.26.0",
    "pypdf>=6.0.0",
]

[project.scripts]
tender-scrapper = "tender_scrapper:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
